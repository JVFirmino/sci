{
  "name": "sci",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright test --reporter=json,html --retries=3 > report.json",
    "report": "npx playwright show-report",
    "login": "npx playwright test --grep @LOGIN --reporter html",
    "admissaoPreliminar": "npx playwright test --grep @ADMISSAO_PRELIMINAR --reporter html"
  },
  "author": "João",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^10.0.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.5.2"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cpf-cnpj-validator": "^1.0.3"
  }
}
